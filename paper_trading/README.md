# Paper Trading Compatibility

The maintained paper-trading package now lives in `trade/`.

Use:
- `trade/` for reusable modules
- `scripts/trade/` for CLI entrypoints

This directory remains only as a compatibility layer for older imports such as `paper_trading.replay`.
